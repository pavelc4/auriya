# Requirements

- A rooted Android device supported by Magisk, KernelSU, or APatch.
- A compatible Android version and `arm64-v8a` device.
- Permission to install the manager and companion applications.
- A kernel exposing the `/proc` and `/sys` nodes required by the selected profile.

Root access is a trust boundary. Review the module source and configuration before installation.
