# First Run

The onboarding flow verifies root access, stores appearance preferences, selects the floating modern navigation style, and marks setup complete. Denying root access prevents the flow from continuing because the manager cannot query or control the daemon safely.
