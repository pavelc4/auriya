# Uninstall

Use the module's uninstall action from the root manager. The uninstall flow stops and removes the manager and companion packages before removing module files. Wait for the countdown to finish before rebooting so Android has time to complete package removal.
