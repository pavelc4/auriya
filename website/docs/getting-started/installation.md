# Installation

1. Download the current module archive and manager APK from the project release.
2. Install the manager APK.
3. Flash the module through the supported root manager.
4. Reboot when the root manager requests it.
5. Open Auriya and complete root authorization and theme setup.

The module starts the daemon during Android boot. The manager reads daemon state through the companion communication layer.
