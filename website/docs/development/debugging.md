# Debugging

Start by identifying the failing boundary: Android UI, companion service, socket communication, daemon subsystem, or kernel node. Capture the command, response, active package, root manager, ROM, kernel, and relevant configuration without publishing private device data.
