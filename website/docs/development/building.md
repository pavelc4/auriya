# Building

```bash
cargo build
cargo test

cd android
./gradlew build

cd ../website
bun install
bun run build
```

Use the repository's pinned Rust, Android SDK, and Bun requirements where available.
