package dev.auriya.app

import android.app.Application

/**
 * Process-wide singletons live here. The scaffold keeps this empty so
 * future phases can attach DI (Hilt) without breaking the build
 * inheritance chain.
 */
class AuriyaApplication : Application()
